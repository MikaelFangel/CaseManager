defmodule CaseManager.Repo.Migrations.AddCascadeDeleteToComments do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    drop(constraint(:comments, "comments_alert_id_fkey"))

    drop(constraint(:comments, "comments_case_id_fkey"))

    alter table(:comments) do
      modify(
        :case_id,
        references(:cases,
          column: :id,
          name: "comments_case_id_fkey",
          type: :uuid,
          prefix: "public",
          on_delete: :delete_all,
          on_update: :update_all
        )
      )

      modify(
        :alert_id,
        references(:alerts,
          column: :id,
          name: "comments_alert_id_fkey",
          type: :uuid,
          prefix: "public",
          on_delete: :delete_all,
          on_update: :update_all
        )
      )
    end
  end

  def down do
    drop(constraint(:comments, "comments_case_id_fkey"))

    drop(constraint(:comments, "comments_alert_id_fkey"))

    alter table(:comments) do
      modify(:alert_id, references(:alerts, column: :id, name: "comments_alert_id_fkey", type: :uuid, prefix: "public"))
      modify(:case_id, references(:cases, column: :id, name: "comments_case_id_fkey", type: :uuid, prefix: "public"))
    end
  end
end
